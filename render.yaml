services:
  - type: web
    name: osint-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot2.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false  # <--- ЭТА СТРОКА ВАЖНА
      - key: DB_PATH
        value: database.db